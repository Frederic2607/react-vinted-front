import React from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

const CheckedSort = () => {
  return (
    <div>
      <FontAwesomeIcon
        icon="sort-numeric-down"
        className="toggle-checked-down"
      />
    </div>
  );
};

export default CheckedSort;
