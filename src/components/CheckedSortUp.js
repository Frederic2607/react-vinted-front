import React from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

const CheckedSortUp = () => {
  return (
    <div>
      <FontAwesomeIcon
        icon="sort-numeric-down-alt"
        className="toggle-checked-up"
      />
    </div>
  );
};

export default CheckedSortUp;
